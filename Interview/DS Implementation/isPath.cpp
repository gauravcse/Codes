#include<iostream>
#include<vector>
#include<list>
#nclude<stack>
#include<algorithm>

using namespace std;

void isPath(vector<list<int> > &g) {
	int v = g.size();
	vector<bool> visit(v,false);
	stack<int> dfs;
}

int main(void) {
	
	return 0;
}